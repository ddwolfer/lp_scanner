# P3 Wash-Trading Analysis Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `analyze-pool.mjs` 的刷量分析接進每日 scanner，只對評分前 N 名（`wash_analysis_top_n = 20`）跑，填 `trader_count / top1_share / pingpong_ratio / lp_trader_overlap / lp_overlap_volume_share`，命中門檻加 `wash_suspect` 並重新評分。

**Architecture:** `metrics/wash.ts` 純函式：輸入帶 `trader`（tx.from）與時間戳的 swap 列與 LP 地址集合，輸出指標。`sources/uniswapV4.ts` 加 `fetchModifyLiquidity`；`sources/traders.ts` 用 `eth_getTransactionByHash` 解析 tx.from（Alchemy key 存在時走 Alchemy，免費方案允許此方法且 500 CU/s；否則走 public RPC 並限制每池最多 2,000 筆最新 swap，flag `wash_sampled`）。run.ts 在評分後新增階段 7：取前 N 名 → 分析 → 更新快照 → 重算排除與評分 → 摘要。

**Spec:** SPEC §5（刷量分析列）、§6（欄位）、§6.2（沿用 analyze-pool.mjs 定義）、§8.1 `wash_suspect`（DECISIONS C2：第三條件為成交量佔比）、§12 P3。

## Global Constraints
- 只讀：getLogs、getTransactionByHash。無簽名。
- 門檻讀 `config/scoring.json`：`wash_top1_share 0.6`、`wash_pingpong_ratio 0.3`、`wash_overlap_volume_share 0.5`、`wash_analysis_top_n 20`。
- 對打定義沿用腳本：同地址相鄰兩筆方向相反且間隔 < 600 秒。
- 交易量以 USDG 側絕對量計。

---

### Task 1: `metrics/wash.ts`（純函式 + 測試）

**Interfaces:**
- `WashSwap = { trader: string; ts: number; dir: 'buy' | 'sell'; volumeUsd: number }`
- `analyzeWash(swaps: WashSwap[], lpAddrs: Set<string>): WashMetrics`
- `WashMetrics = { traderCount, top1Share, pingpongRatio, lpTraderOverlap, lpOverlapVolumeShare, topTraders: { addr, n, buy, sell, share }[] (前 5), hourly: Record<string, number> }`
- 空 swaps → 全 0。

```ts
// tests/wash.test.ts
import { it, expect } from 'vitest'
import { analyzeWash } from '../scanner/metrics/wash.js'
const s = (trader: string, ts: number, dir: 'buy' | 'sell', v: number) => ({ trader, ts, dir, volumeUsd: v })
it('集中度、對打、LP 重疊', () => {
  const m = analyzeWash([s('a', 0, 'buy', 60), s('a', 100, 'sell', 60), s('b', 200, 'buy', 40), s('c', 5000, 'sell', 40)], new Set(['a']))
  expect(m.traderCount).toBe(3)
  expect(m.top1Share).toBeCloseTo(120 / 200, 9)
  expect(m.pingpongRatio).toBeCloseTo(1 / 4, 9)
  expect(m.lpTraderOverlap).toBe(1)
  expect(m.lpOverlapVolumeShare).toBeCloseTo(0.6, 9)
  expect(m.topTraders[0]).toMatchObject({ addr: 'a', n: 2, buy: 1, sell: 1 })
})
it('空輸入', () => { expect(analyzeWash([], new Set()).traderCount).toBe(0) })
```

```ts
// scanner/metrics/wash.ts — 沿用 analyze-pool.mjs 的定義（SPEC §6.2），純函式
export interface WashSwap { trader: string; ts: number; dir: 'buy' | 'sell'; volumeUsd: number }
export interface WashMetrics { traderCount: number; top1Share: number; pingpongRatio: number; lpTraderOverlap: number; lpOverlapVolumeShare: number; topTraders: { addr: string; n: number; buy: number; sell: number; share: number }[]; hourly: Record<string, number> }
export function analyzeWash(swaps: WashSwap[], lpAddrs: Set<string>): WashMetrics {
  if (!swaps.length) return { traderCount: 0, top1Share: 0, pingpongRatio: 0, lpTraderOverlap: 0, lpOverlapVolumeShare: 0, topTraders: [], hourly: {} }
  const by = new Map<string, { n: number; buy: number; sell: number; vol: number }>()
  let total = 0
  for (const s of swaps) { const t = by.get(s.trader) ?? { n: 0, buy: 0, sell: 0, vol: 0 }; t.n++; s.dir === 'buy' ? t.buy++ : t.sell++; t.vol += s.volumeUsd; total += s.volumeUsd; by.set(s.trader, t) }
  const sorted = [...by.entries()].sort((a, b) => b[1].vol - a[1].vol)
  const share = (v: number) => total > 0 ? v / total : 0
  const rows = [...swaps].sort((a, b) => a.ts - b.ts); const last = new Map<string, WashSwap>(); let pp = 0
  for (const r of rows) { const p = last.get(r.trader); if (p && p.dir !== r.dir && r.ts - p.ts < 600) pp++; last.set(r.trader, r) }
  const hourly: Record<string, number> = {}
  for (const r of rows) { const h = new Date(r.ts * 1000).toISOString().slice(0, 13); hourly[h] = (hourly[h] ?? 0) + 1 }
  const overlap = sorted.filter(([a]) => lpAddrs.has(a))
  return { traderCount: by.size, top1Share: share(sorted[0][1].vol), pingpongRatio: pp / rows.length, lpTraderOverlap: overlap.length,
    lpOverlapVolumeShare: share(overlap.reduce((s, [, t]) => s + t.vol, 0)),
    topTraders: sorted.slice(0, 5).map(([addr, t]) => ({ addr, n: t.n, buy: t.buy, sell: t.sell, share: share(t.vol) })), hourly }
}
```

Commit: `feat: wash analysis metrics`

### Task 2: 來源：ModifyLiquidity log 與 tx.from

- `uniswapV4.ts` 加 `fetchModifyLiquidity(rpc, poolId, from, to): Promise<{ txHash: string; sender: string }[]>`。
- 新檔 `sources/traders.ts`：`resolveTxFrom(rpc, hashes: string[]): Promise<Map<string, string>>`（去重、用 `rpc.call(() => rpc.client.getTransaction({ hash }))`，回 lowercase from）。
- `makeTraderRpc(usage)`：有 `ALCHEMY_KEY` → `makeRpc({ url: CHAIN.alchemyRpc(key), source: 'alchemy', concurrency: 8, minGapMs: 40 })`；否則 public rpc。
- 測試 `resolveTxFrom` 用假 rpc（`call` 直接執行，`client.getTransaction` 回固定值）。

Commit: `feat: modify-liquidity logs and tx.from resolution`

### Task 3: run.ts 階段 7 + steps

- `steps.ts` 加 `updateWash(db, poolId, date, m: WashMetrics, extraFlags, excluded)`：更新五個欄位 + `flags` + `excluded`，`wash_detail` 存 JSON（新增欄位 `wash_detail TEXT` 進 schema：topTraders 與 hourly，dashboard 用）。
- run.ts：評分後 `top = cands sorted by score desc slice(0, wash_analysis_top_n)`；對每個：`fetchSwaps`（當日；已抓過但沒留，重抓 9 次）、`fetchModifyLiquidity`、`resolveTxFrom`（swap + lp 的 tx hash；public RPC 時取最新 2,000 筆 swap 並 flag `wash_sampled`）→ `WashSwap[]`（dir：股票側 amount < 0 為使用者買入股票 → 'buy'）→ `analyzeWash` → `hardExclusions` 只重算 wash 條件（直接比對門檻）→ `updateWash`。之後對所有候選重跑 `scorePools`（被 wash 排除的不進）。摘要 `traderCount` 從快照讀。
- `SnapshotRow` 不變；摘要行「交易者」自然有值。

Commit: `feat: wash analysis for top-N pools in daily scan`

### Task 4: DECISIONS D24–D26、實跑、合併
- D24 tx.from 走 Alchemy（免費方案允許 getTransactionByHash）；D25 public RPC 取樣上限 2,000；D26 wash 只對前 N 名跑、被排除後重新評分。
- `pnpm scan --sim-only` 需同時能跑 wash（讀 DB 候選、重抓 swap）：`--sim-only` 改名語意為「跳過每日抓取階段」，wash 階段照跑。
- 實跑、記錄呼叫次數。
